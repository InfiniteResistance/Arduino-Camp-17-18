const int motorOnePin = 11;
const int motorTwoPin = 9;
const int motorThreePin = 5;
const int motorFourPin = 10;
const int motorFivePin = 6;
const int motorSixPin = 3;

void setup() {
  pinMode(motorOnePin, OUTPUT);
  pinMode(motorTwoPin, OUTPUT);
  pinMode(motorThreePin, OUTPUT);
  pinMode(motorFourPin, OUTPUT);
  pinMode(motorFivePin, OUTPUT);
  pinMode(motorSixPin, OUTPUT);

  digitalWrite(motorOnePin, HIGH);
  digitalWrite(motorTwoPin, HIGH);
  digitalWrite(motorThreePin, HIGH);
  digitalWrite(motorFourPin, HIGH);
  digitalWrite(motorFivePin, HIGH);
  digitalWrite(motorSixPin, HIGH);
}

void loop() {
  //V O I D 

}
